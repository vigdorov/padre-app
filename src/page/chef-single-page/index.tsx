import React from 'react';

interface IProps {
    children: any;
    id: any;
}

const ChefSinglePage = (props: IProps) => {
    return (
        <div>
            Chef single page
        </div>
    );
};

export default ChefSinglePage;